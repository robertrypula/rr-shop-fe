import { ExternalIdPipe } from './external-id.pipe';

describe('ExternalIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ExternalIdPipe();
    expect(pipe).toBeTruthy();
  });
});
