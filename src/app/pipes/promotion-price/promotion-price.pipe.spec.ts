import { PromotionPricePipe } from './promotion-price.pipe';

describe('PromotionPricePipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionPricePipe();
    expect(pipe).toBeTruthy();
  });
});
