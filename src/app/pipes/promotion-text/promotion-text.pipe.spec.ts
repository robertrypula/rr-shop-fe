import { PromotionTextPipe } from './promotion-text.pipe';

describe('PromotionTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionTextPipe();
    expect(pipe).toBeTruthy();
  });
});
