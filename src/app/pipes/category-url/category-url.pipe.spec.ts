import { CategoryUrlPipe } from './category-url.pipe';

describe('CategoryUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
